export class UserAccount {
  id: number;
  spotifyId: string;
  displayName: string;
  loginToken: string;
  accessToken: string;
  refreshToken: string;
  selectedDevice: string;
  created: Date;
  updated: Date;
}
